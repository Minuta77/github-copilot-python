# Sudoku Flask Application

## How to Run Tests

To execute the tests for this project, use the following command:

```bash
c:/Users/fixcmo5/github-copilot-python/starter/.venv/Scripts/python.exe -m pytest tests
```

This will run all the tests located in the `tests/` directory and ensure that the application logic is functioning as expected.